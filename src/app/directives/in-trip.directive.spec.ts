import { InTripDirective } from './in-trip.directive';

describe('InTripDirective', () => {
  it('should create an instance', () => {
    const directive = new InTripDirective();
    expect(directive).toBeTruthy();
  });
});
